# japanese-quiz-game

A Japanese quiz game built with SvelteKit, deployed to Cloudflare Workers with a D1 database.

> **Project status: scaffolding.** The toolchain, Cloudflare bindings, and test setup are wired up, but the quiz itself is not built yet. What ships today is the generated starter plus demo routes. See [Known gaps](#known-gaps) before you start.

## Stack

| Concern                | Choice                                                |
| ---------------------- | ----------------------------------------------------- |
| Framework              | SvelteKit (`next`) with Svelte 5, runes forced on     |
| Hosting                | Cloudflare Workers via `@sveltejs/adapter-cloudflare` |
| Database               | Cloudflare D1 + Drizzle ORM                           |
| i18n                   | Paraglide JS — `en` (base) and `ja`                   |
| Unit / component tests | Vitest (browser + node projects)                      |
| E2E tests              | Playwright                                            |
| Package manager        | pnpm                                                  |

SvelteKit is configured **inline in [`vite.config.ts`](vite.config.ts)** — there is no `svelte.config.js`. Experimental `async` and `remoteFunctions` are enabled there.

## Prerequisites

- **Node.js 24+** (developed on v24.16.0)
- **pnpm 11+** (developed on 11.23.0) — `npm install -g pnpm`
- A **Cloudflare account** if you want to touch the database or deploy. You can run the UI locally without one.

## Getting started

```bash
git clone <repo-url> japanese-quiz-game && cd japanese-quiz-game
```

```bash
pnpm install
```

```bash
pnpm dev
```

Seed the local database so the game has questions to ask:

```bash
pnpm db:reset:local
```

```bash
pnpm dev
```

The app is on http://localhost:5173. Add `--open` to launch a browser: `pnpm dev -- --open`.

That is the whole setup. `vite dev` binds a **local** SQLite database under `.wrangler/state`, so
the full game — the question bank, the leaderboard, recording a run — works with no Cloudflare
account and no credentials. The Cloudflare steps below are only needed to deploy or to touch the
shared database.

Skipping the seed leaves both courses reading `OUT OF SERVICE` on the SELECT COURSE screen: the
schema is empty, not broken.

> **Run `pnpm dev` or `pnpm build` before `pnpm check`.** `src/lib/paraglide/` is generated by the Vite plugin and is gitignored, so on a fresh clone it does not exist yet. Type-checking before the first build reports a wall of "Cannot find module '#lib/paraglide/…'" errors that resolve themselves once the directory is generated.

## Cloudflare setup

Two separate credentials are involved, and they are easy to confuse:

1. **Wrangler's login** — an OAuth session used by `wrangler d1 create`, `wrangler types`, and `wrangler deploy`. Set it up once with `pnpm exec wrangler login`, verify with `pnpm exec wrangler whoami`.
2. **A D1 API token in `.env`** — used _only_ by Drizzle Kit, which talks to D1 over HTTP rather than through the Worker binding.

### Configure `.env`

Copy the template and fill it in:

```bash
cp .env.example .env
```

| Variable                 | Where it comes from                                                                                       |
| ------------------------ | --------------------------------------------------------------------------------------------------------- |
| `CLOUDFLARE_ACCOUNT_ID`  | `wrangler whoami`, or the sidebar of any domain's dashboard overview                                      |
| `CLOUDFLARE_DATABASE_ID` | The UUID printed by `wrangler d1 create` — must match `database_id` in [`wrangler.jsonc`](wrangler.jsonc) |
| `CLOUDFLARE_D1_TOKEN`    | Dashboard → My Profile → API Tokens → Create Custom Token, with **Account → D1 → Edit** permission        |

Every `db:*` script throws immediately if any of the three is missing. `.env` is gitignored; never commit it.

### Point at a database

[`wrangler.jsonc`](wrangler.jsonc) declares a binding named `quizdb` against a specific `database_id`. **This does not create anything** — Wrangler looks the UUID up in your account and fails if it is not there.

If you are working against your own Cloudflare account, create your own database and use its ID:

```bash
pnpm exec wrangler d1 create quizdb
```

Put the returned UUID in both `wrangler.jsonc` (`database_id`) and `.env` (`CLOUDFLARE_DATABASE_ID`). D1 UUIDs are per-account and are not portable between contributors.

### Apply the schema

The schema lives in [`src/lib/server/db/schema.ts`](src/lib/server/db/schema.ts) and its migrations
are committed under [`drizzle/`](drizzle/). To apply them to the remote database:

```bash
pnpm db:migrate
```

After changing the schema, generate a new migration with `pnpm db:generate` and commit it. For
throwaway local iteration, `pnpm db:push` skips migration files and pushes the schema straight to
D1; use `db:generate` + `db:migrate` for anything that lands on main.

### Seed the question bank

The bank is authored as typed TypeScript in
[`src/lib/game/official-data.ts`](src/lib/game/official-data.ts) and compiled to SQL by
[`scripts/generate-seed.ts`](scripts/generate-seed.ts), so it is checked by the same compiler as
the rest of the game. Regenerate [`drizzle/seed.sql`](drizzle/seed.sql) after editing the bank:

```bash
pnpm db:seed:gen
```

The seed replaces the content tables and leaves `players`, `sessions` and `session_answers`
untouched, so re-seeding never erases a leaderboard.

```bash
pnpm db:seed:local    # the SQLite file behind `pnpm dev`
pnpm db:seed:remote   # the shared D1 — needs wrangler login
```

> The bank contains official JLPT N4 & N3 questions. See [PRODUCT.md](PRODUCT.md).

After changing bindings in `wrangler.jsonc`, regenerate the ambient types:

```bash
pnpm gen
```

This rewrites `worker-configuration.d.ts`. `pnpm build` and `pnpm check` both run `wrangler types --check` and will fail if it is stale.

## Scripts

| Script                                                      | What it does                                      |
| ----------------------------------------------------------- | ------------------------------------------------- |
| `pnpm dev`                                                  | Vite dev server                                   |
| `pnpm build`                                                | Type-checks bindings, then builds for Workers     |
| `pnpm preview`                                              | Runs the built Worker locally on port 4173        |
| `pnpm check`                                                | `wrangler types --check` + `svelte-check`         |
| `pnpm lint`                                                 | Prettier check + ESLint                           |
| `pnpm format`                                               | Prettier write                                    |
| `pnpm test:unit`                                            | Vitest (watch)                                    |
| `pnpm test:e2e`                                             | Installs browsers, then runs Playwright           |
| `pnpm test`                                                 | Unit tests once, then E2E                         |
| `pnpm gen`                                                  | Regenerate `worker-configuration.d.ts`            |
| `pnpm db:generate` / `db:migrate` / `db:push` / `db:studio` | Drizzle Kit against D1                            |
| `pnpm db:reset:local`                                       | Migrate + seed the local dev database             |
| `pnpm db:seed:gen`                                          | Rebuild `drizzle/seed.sql` from the question bank |
| `pnpm db:seed:local` / `db:seed:remote`                     | Apply the seed to the local or shared database    |

## Project layout

```
src/
  app.d.ts              App.Platform + App.Locals (db, repos) types
  hooks.server.ts       Puts Drizzle and the repositories on event.locals; Paraglide
  hooks.ts              Reroute hook that strips the locale prefix from URLs
  lib/
    game/               THE CLIENT HALF — no drizzle, no server imports
      types.ts          DTOs and constants shared by both halves
      scoring.ts        The scoring rules, run by the browser AND the server
      run.svelte.ts     The cabinet's run state: lives, clock, streak, answer log
      api.ts            The only place a component reaches the network
      official-data.ts  The authored question bank; source for drizzle/seed.sql
    server/
      db/               Drizzle: schema, migrations, per-table repositories
      game/
        queries.ts      Reads: topics, questions, counts, leaderboard
        record-session.ts  Regrades a posted run and writes it
    paraglide/          Generated at build time — do not edit or commit
  routes/
    +page.svelte        Attract screen (board + demo reel, server-rendered)
    play/               SELECT COURSE, the run, GAME OVER, name entry
    ranking/            The leaderboard
    members/            Member contributions roll, linked from CREDIT FREE PLAY on the title screen
    api/                topics, questions, leaderboard, sessions
scripts/
  generate-seed.ts      official-data.ts -> drizzle/seed.sql
drizzle/
  *.sql, seed.sql       Migrations and the question-bank seed
messages/
  en.json, ja.json      Translation sources
tests/
  game-loop.spec.ts     End-to-end: title -> run -> name -> board
```

### How the two halves meet

Components never import from `src/lib/server/`, and never build a URL. Anything a page can know
before the player acts is loaded in a `+page.server.ts` and rendered server-side; anything it can
only learn afterwards goes through `src/lib/game/api.ts` to `/api/*`.

The one rule worth stating outright: **the browser does not decide a score.** A finished run posts
its answer log — question id, chosen choice id, milliseconds spent — and
`record-session.ts` regrades every answer against `choices.is_correct`, replays the streak and
multiplier through `scoring.ts`, and writes the total it computed itself. The client's own tally
exists only to be displayed while the run is happening.

## Internationalization

Messages live in [`messages/en.json`](messages/en.json) and [`messages/ja.json`](messages/ja.json), configured by [`project.inlang/settings.json`](project.inlang/settings.json). `en` is the base locale.

Add a key to both files, then import it from the generated runtime:

```ts
import { m } from '#lib/paraglide/messages.js'

m.hello_world({ name: 'world' })
```

`src/lib/paraglide/` is regenerated by the Vite plugin on every build — treat it as build output.

The `#lib/*` import alias maps to `./src/lib/*` (see `imports` in `package.json`). **Include the `.js` extension.** The mapping is a literal substitution with no extension inference, so `#lib/paraglide/messages.js` type-checks and `#lib/paraglide/messages` does not.

## Code style

Formatting is Prettier, configured in [`prettier.config.js`](prettier.config.js): tabs, single quotes, **no semicolons**, trailing commas, 100-column width. Run `pnpm format` before pushing. Prettier only reformats — it never renames anything.

[`eslint.config.js`](eslint.config.js) adds `@typescript-eslint/naming-convention` on top of the recommended sets:

| Kind                              | Expected                                                                  |
| --------------------------------- | ------------------------------------------------------------------------- |
| variables                         | `camelCase`, `UPPER_CASE`, `PascalCase`                                   |
| functions, parameters, properties | `camelCase`                                                               |
| types, interfaces, classes        | `PascalCase`                                                              |
| exported functions                | also `UPPER_CASE`, for `+server.ts` handlers like `export function GET()` |

The rule is scoped to `src/**` only — root build config, generated code, and `tests/` are exempt. It has **no autofixer**; renaming is a manual refactor (F2 in VS Code updates every reference).

snake_case is allowed in the places where it is the correct spelling rather than a mistake:

- **Object literal keys** — API payloads, D1 column names, env-shaped config. `{ user_id: 1 }` is fine.
- **Destructured bindings** — `const { access_token } = await res.json()` needs no rename at the boundary.
- **Keys that cannot be identifiers** — `'x-api-key'`, `'Content-Type'`.

It is deliberately **not** allowed on `typeProperty`, so interfaces and type aliases stay camelCase. The one place this bites is typing a payload inline:

```ts
// eslint-disable-next-line @typescript-eslint/naming-convention -- external API shape
const { access_token } = (await res.json()) as { access_token: string }
```

## Testing

Vitest runs two projects, split by filename:

- **client** — `src/**/*.svelte.{test,spec}.ts`, in headless Chromium via Playwright
- **server** — everything else under `src/**/*.{test,spec}.ts`, in Node

Note that `expect.requireAssertions` is on, so a test that asserts nothing fails.

Playwright's `testDir` is `./tests`, `baseURL` is `http://localhost:4173`, and a `webServer` entry
starts `vite dev` on that port for you (an already-running server on 4173 is reused outside CI).

[`tests/game-loop.spec.ts`](tests/game-loop.spec.ts) drives the real app against the real local
database: it reads the answer key from `/api/questions`, plays a run in the browser, types a name,
and checks the score reaches `/ranking`. **Seed the local database first** with
`pnpm db:reset:local` — on an empty bank the game has nothing to ask and the suite fails.

CI runs Playwright only, on push and PR to `main`/`master` ([workflow](.github/workflows/playwright.yml)). Reports upload as an artifact.

## Deploying

```bash
pnpm build
```

```bash
pnpm exec wrangler deploy
```

`workers_dev` and `preview_urls` are both enabled, so you get a `*.workers.dev` URL and per-version preview URLs.

## Known gaps

Contributors should know these are outstanding rather than assume something is broken.

**Current check status:** `pnpm lint` and `pnpm check` both pass clean, before _and_ after a build. `vite build` succeeds and `wrangler types --check` reports the bindings up to date (`quizdb` is present in `Env`).

### Type-checking notes

Getting to zero errors took four fixes worth knowing about, since they explain some non-obvious config:

- **`checkJs: false` in [`tsconfig.json`](tsconfig.json).** SvelteKit defaults it to `true`, and `wrangler types` writes a `GlobalProps.mainModule` entry pointing at `./.svelte-kit/cloudflare/_worker` — together those pulled the entire compiled bundle into the type program, producing ~493 phantom errors after every build. This project has no hand-written `.js`, so nothing of ours goes unchecked.
- **`#lib/*` imports need the `.js` extension.** The alias is a literal substitution with no extension inference.
- **`Handle` and `Reroute` come from `@sveltejs/kit/hooks`, not `@sveltejs/kit`.** They moved subpath in kit `3.0.0-next.25`; the `sv` scaffold still imports them from the old location.
- **`RequestEvent.request` is read-only in kit 3.** Paraglide's scaffolded middleware assigns to it, so [`hooks.server.ts`](src/hooks.server.ts) carries a narrow cast with a comment. Runtime behaviour is unchanged. Revisit when Paraglide ships a kit-3-compatible middleware.

Generated files that builds rewrite in their own style — `worker-configuration.d.ts`, `project.inlang/.meta.json`, `project.inlang/README.md` — are listed in [`.prettierignore`](.prettierignore) so a build no longer breaks `pnpm lint`. (`src/lib/paraglide/` is already exempt via `.gitignore`, which Prettier honours.)

### Other

- **The question bank is official content.** The items in `official-data.ts` provide official JLPT questions across N4 and N3 levels.
- **The answer key ships to the browser.** `/api/questions` returns `isCorrect` on every choice, so the answer is readable in devtools. This is deliberate — the run screen has to give a verdict the instant a finger lands, and a round trip per question would put the network inside a ten-second clock. The board is protected at `POST /api/sessions` instead, which regrades server-side. Withholding the key would mean grading each answer over the wire, which is a redesign of the round, not a patch.
- **Runs are recorded, not resumable.** A session row is written once, at the end. A player who closes the tab mid-run leaves no trace, and `status: 'abandoned'` is accepted by the endpoint but never sent.
- **Template scaffolding is still present.** `src/routes/demo/` and `src/lib/vitest-examples/` are sample code, and `src/routes/demo/playwright/page.svelte.e2e.ts` never runs because Playwright's `testDir` is `./tests`. All safe to delete.
- **CI triggers on `main`/`master`, but this repo's trunk is `development`** ([workflow](.github/workflows/playwright.yml)), so the suite does not currently run on pull requests. Adding `development` to both trigger lists is the fix.
- **CI runs Playwright only** — no `lint`, `check`, or unit tests.
- **`pnpm lint` reports pre-existing errors**, all `@typescript-eslint/naming-convention` on snake_case methods and locals in `src/lib/server/db/`. The layer's convention and the repo's lint config disagree; renaming has no autofixer and touches every call site, so it wants its own change.
- **`.npmrc` sets `engine-strict=true` but `package.json` has no `engines` field**, so the Node/pnpm versions above are not actually enforced.

## Regenerating this scaffold

For reference, the project was created with:

```bash
pnpm dlx sv@0.17.0 create --template minimal --types ts --add prettier eslint vitest="usages:unit,component" playwright sveltekit-adapter="adapter:cloudflare+cfTarget:workers" drizzle="database:d1" paraglide="languageTags:en, ja+demo:yes" experimental="versions:kit+features:async,remoteFunctions,explicitEnvironmentVariables,handleRenderingErrors" --install pnpm japanese-quiz-game
```
